var foo = (function () {
    function foo() {
function bar(foo) {
            return "foo";
        }
        ; ;
        var test = bar("test");
        var tester = bar();
        tester = bar("test");
    }
    return foo;
})();
